# Ask the user for an integer input
n = int(input("Enter an Integer: "))

# Perform the AND operation between n and 30
result = n & 30

# Print the result in the specified format
print(f"{n} AND 30 = {result}")
